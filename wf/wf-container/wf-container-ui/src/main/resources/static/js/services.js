(function() {
    /*
    var ItemFactory = function($resource) {
        return $resource('/items/:id', {
            id: '@id'
        }, {
            update: {
                method: "PUT"
            },
            remove: {
                method: "DELETE"
            }
        });
    };

    ItemFactory.$inject = ['$resource'];
    angular.module("container.services").factory("Item", ItemFactory);
    */
    /*
    var WorkflowFactory = function() {
        return new Worflow();
    }
    */

    //angular.module("container.services").factory("Workflow", WorkflowFactory);
    /*
    var StepFactory = function() {
        return new Step();
    }
    */

    //angular.module("container.services").factory("Step", StepFactory);

}());